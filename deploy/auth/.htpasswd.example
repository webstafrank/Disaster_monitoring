# Format: one "username:hash" line per user (apr1 or bcrypt). This is a SAMPLE.
# Do NOT use it in production. Generate the real deploy/auth/.htpasswd with:
#   ./scripts/set_password.sh <username>
# The example line below is user "demo" with password "change-me" (apr1). Replace it.
demo:$apr1$s09FrSdU$fxxNaJe4cAEHkM1vknJIS.
